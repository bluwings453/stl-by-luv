#include<bits/stdc++.h>
using namespace std;

int main()
{
    auto a=1.0;
    cout<<a<<endl;
    // auto keyword determines the type of datatype itself 
    // without being explicity coded to it 
    
    return 0;
}